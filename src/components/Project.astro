---
type ProjectProps = {
  title: string;
  description: string;
  link: string;
};
const { title, description, link } = Astro.props;
---
<div class='flex flex-col gap-3 w-44'>
  <a href={link} target='_blank'><div class='font-normal underline'>{title}</div></a>
  <div>{description}</div>
</div>